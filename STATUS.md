# Server-Host V2 status

The active authority is [`docs/v2/STATUS.md`](docs/v2/STATUS.md).

Gate 2A exact identity, Gate 2B read-only discovery and Gate 2C live
relationships are device verified. Closure result
`V2-G2C-CONTINUOUS-MENU-MAP-DEVICE-PASS-008` binds the immutable `.007`
continuous transcript to the user's exact action record: capture 1 in the main
menu, capture 2 after natural entry to TheIsland, and capture 3 in the same
TheIsland without travel. Discovery advanced `1->2->3`; world generation
advanced `1->2` with prior-world invalidation and then remained 2 with no
prior-world invalidation. Every Engine/Viewport/World/GWorld/definitions and
optional relationship passed, pre-host `net_driver=none` remained normal, no
visible gameplay/input/audio regression occurred, and
`hooks=0 engine_calls=0 mutation=0`.

Gate 2C is closed. Gate 3 is unblocked but not started. The exact next bounded
workflow is Gate 3A evidence-only research for the current `FIOSAsyncTask`
game-thread scheduler contract (`ABI-015`); it must publish a reviewed contract
card and test plan before any dispatcher implementation.

Legacy runtime, the root Legacy Makefile, old packages and `HostingRuntime` are
outside the workflow and remain unchanged. Death/respawn is a reproduced stock
baseline limitation and is not a Gate 2C PASS/FAIL condition.
